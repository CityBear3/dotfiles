# dotfiles

Personal configuration sources for independently managed tools.

- [`claude/`](claude/) — Claude Code configuration, skills, agents, and statusline.
- [`codex/`](codex/) — Codex configuration fragment, global guidance, custom agents, and personal skills.

Each tree owns only its corresponding tool. Codex assets do not import the Claude tree at runtime.
